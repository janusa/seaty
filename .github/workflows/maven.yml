name: Verify

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      # The Biome frontend gate (verify) downloads the pinned per-OS binary into ~/.cache/biome
      # itself, so no install step is needed - only this cache, so it isn't re-fetched every run.
      # On a version bump the exact key misses; restore-keys reuses the newest cache and the
      # antrun <get skipexisting> fetches just the new version.
      - name: Cache Biome binary
        uses: actions/cache@v4
        with:
          path: ~/.cache/biome
          key: ${{ runner.os }}-biome-${{ hashFiles('pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-biome-
      - name: Install Playwright Chromium + OS deps
        run: >
          ./mvnw -B org.codehaus.mojo:exec-maven-plugin:3.6.3:java
          -Dexec.mainClass=com.microsoft.playwright.CLI
          -Dexec.args="install --with-deps chromium"
          -Dexec.classpathScope=test
          --file pom.xml
      - name: Build with Maven
        run: ./mvnw -B verify --file pom.xml
      - name: Publish jar for deployment
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: seaty-jar
          path: target/seaty-*.jar
          if-no-files-found: error
          retention-days: 7
